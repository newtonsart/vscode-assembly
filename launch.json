{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "GDB",
            "program": "${workspaceRoot}/${fileBasenameNoExtension}",
            "stopOnEntry": true,
            "preLaunchTask": "assemble"
        }
    ]
}
